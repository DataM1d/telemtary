syntax = "proto3";

package telemetry;

option go_package = "telemetry-engine/internal/pb";

message Metric {
    uint32 id = 1;
    double value = 2;
    uint32 type = 3;
    int64 timestamp = 4;
}

message MetricBatch {
    repeated Metric metrics = 1;
}